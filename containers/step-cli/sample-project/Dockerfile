FROM cleanstart/step-cli:latest-dev

# Base image already contains:
# - Step CLI binary
# - Non-root user (clnstrt)
# - Proper directory structure
# - Environment variables
# - Health check
# - Entrypoint and default command

# Add Cleanstart labels
LABEL Cleanstart.main.package="step-cli"
LABEL org.opencontainers.image.authors="Cleanstart Team https://www.cleanstart.com/"
LABEL org.opencontainers.image.vendor="Cleanstart"
