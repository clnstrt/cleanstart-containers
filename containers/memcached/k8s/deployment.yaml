apiVersion: apps/v1
kind: Deployment
metadata:
  name: memcached-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: memcached-app
  template:
    metadata:
      labels:
        app: memcached-app
    spec:
      containers:
      - name: memcached-app
        image: us-central1-docker.pkg.dev/consumption-442810/testing-community-images/memcached-app:latest
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30